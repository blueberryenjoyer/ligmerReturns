function sum(num2, num1) {
    outputnumber = (num2 - num1)
    return (outputnumber)
}

console.log(sum(69, 420))
//69 becomes num2 because it is like this
//sum(69, 420)
//then the function is like sum(num1, num2)
//so the numbers are passed when it is called